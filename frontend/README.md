# Task Scheduler Frontend

This is a simple React frontend for the Task Scheduler backend.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the app:
   ```bash
   npm start
   ```

The app expects the backend API to be available at `http://localhost:8080/api/tasks`.
